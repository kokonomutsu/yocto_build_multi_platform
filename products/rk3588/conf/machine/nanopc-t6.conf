#@TYPE: Machine
#@NAME: NanoPC-T6
#@DESCRIPTION: NanoPC-T6 RK3588 board from FriendlyElec

require conf/machine/include/rk3588.inc

# Kernel configuration
# Note: Device tree may need adjustment based on actual kernel support
KERNEL_DEVICETREE ?= "rockchip/rk3588-nanopc-t6.dtb"

# U-Boot configuration
# Note: May need adjustment if defconfig name differs
UBOOT_MACHINE ?= "nanopc-t6-rk3588_defconfig"

MACHINE_EXTRA_RRECOMMENDS += "kernel-modules"

MACHINE_FEATURES += "wifi bluetooth"

# Use linux-yocto as kernel provider (from rockchip-defaults.inc)
# May need kernel recipe with COMPATIBLE_MACHINE for nanopc-t6
PREFERRED_PROVIDER_virtual/kernel ?= "linux-yocto"
